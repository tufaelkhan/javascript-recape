const greetNextLevelDeveloper = (name) => {
    const greetMessage = `Hello ${name} ,welcome to next level developer`
    console.log(greetMessage);
}
// greetNextLevelDeveloper('tufael')

export default greetNextLevelDeveloper;